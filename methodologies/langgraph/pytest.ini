[pytest]
testpaths = tests
filterwarnings =
    # create_react_agent moved to langchain.agents in LangGraph V1 but the
    # functional shim in langgraph.prebuilt is supported through V2; we
    # use it deliberately to avoid pulling in the full langchain package.
    ignore::langgraph.warnings.LangGraphDeprecatedSinceV10
